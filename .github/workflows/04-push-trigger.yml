name: Push-trigger
on: 
  push:
    branches:
      - main
      - develop
      - 'release/**'

  pull_request:
    branches:
      - main
      - develop
      - 'release/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build-step
        run: echo "Running the build"